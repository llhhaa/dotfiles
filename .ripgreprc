# Search hidden files and directories.
--hidden

# Follow symbolic links.
--follow

# Don't respect ignore files (.gitignore, .ignore, etc.).
# --no-ignore

# Exclude directories.
--glob=!{.git,.svn,.tldr,.bundle,node_modules,Trash,tmp,vendor,cassettes,log}

# Exclude file types.
# --glob=!*.{lock,tmp}

# Exclude files.
--glob=!{package-lock.json}

# Don't print lines longer than this limit.
--max-columns=1000
--max-columns-preview

# Searches case insensitively.
--smart-case

# Sort by file path.
--sort=path
